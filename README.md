## Install packages

`npm install && serverless dynamodb install`

## Tests

`npm test`

## Runs locally

`npm start`

## API Docs

On file `openapi.yml`

## Deploy

`npm deploy:<homolog|production>`
